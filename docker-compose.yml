services:
  postgres:
    image: postgres:16
    container_name: cocktailOps-postgres
    environment:
      POSTGRES_DB: cocktailOps_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - "5433:5432"
    volumes:
      - cocktailops_postgres_data:/var/lib/postgresql/data

volumes:
  cocktailops_postgres_data: