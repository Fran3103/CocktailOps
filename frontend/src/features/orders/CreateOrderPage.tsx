import { PagePlaceholder } from "../../shared/components/placeholders/PagePlaceHolder";

export function CreateOrderPage() {
  return (
    <PagePlaceholder
      title="Nueva orden"
      description="Creación de una orden para evento."
    />
  );
}